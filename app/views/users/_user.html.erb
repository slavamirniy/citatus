<li>
  <%= gravatar_for user %>
  <%= link_to user.name, user %>
    <% if current_user.admin? && !(current_user == (user)) %>
    <br>
    <b> <%= link_to "Уничтожить!", user, method: :delete,
                                  data: { confirm: "Точно?" } %></b>
  <% end %>

</li>